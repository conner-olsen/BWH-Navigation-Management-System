
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Docker Compose Up",
            "type": "shell",
            "command": "docker-compose -f docker-compose.wpi.yaml up -d",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        },
        {
            "label": "Docker Compose Down",
            "type": "shell",
            "command": "docker-compose -f docker-compose.wpi.yaml down",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
