setup:
  addons:
    - plan: heroku-postgresql:essential-0
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    YARN_PRODUCTION: false
run:
  web: yarn workspace database run migrate:deploy && yarn workspace backend run deploy & yarn workspace frontend run deploy